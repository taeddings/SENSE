bcrypt==5.0.0
Flask==3.0.0

# Agent0 integration dependencies (enhanced rewards)
nltk>=3.8.0  # For BLEU score calculation in diversity penalty
scikit-learn>=1.3.0  # For agglomerative clustering in diversity penalty

# agent-zero integration dependencies (multi-session terminal)
paramiko>=3.3.0  # Optional: For SSH session support

# Testing dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0